{
  "name": "bkit_checkpoint_list",
  "description": "List saved checkpoints. Optionally filter by feature.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "feature": {
        "type": "string",
        "description": "Filter checkpoints by feature."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 50,
        "default": 20
      }
    },
    "additionalProperties": false
  }
}